extends layout

block content
  h1= title
  - let image_names = results
    table
        thead
            tr 
                th(scope="col") flowers_type 
                th(scope="col") color 
                th(scope="col") cost
        tbody        
            - for(let i=0;i<image_names.length;i++){
                tr
                    td  #{image_names[i].flowers_type}
                    td  #{image_names[i].color}
                    td  #{image_names[i].cost}
            - }