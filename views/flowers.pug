extends layout

block content
  h1= title
  //- const restaurant = ['Room_type','Duration','Cost']
  - let items = results; 
  //- - let results;
  - let a;
  script.
    function buy(x){
      alert('Purchased Item: ' + x)
   } 
  div(class='container-fluid')
    div(class='row')
      each a, index in items 
        div(class='col-sm-3' onclick='buy('+index+')') #{a} 
      